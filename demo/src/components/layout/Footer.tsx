export function Footer() {
  return (
    <footer className="border-t border-line bg-[#0a1022]">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-6 py-6 text-xs text-slate-400 md:flex-row md:items-center md:justify-between">
        <p>Signals are model outputs for demo/research only, not investment advice.</p>
        <p>Built with React, Recharts, ReactFlow, Framer Motion, Lightweight Charts.</p>
      </div>
    </footer>
  );
}
